import datetime

class PacketLoad(list):
    def __init__(self):
        self.StartTime = datetime.datetime.utcnow()
        self.PacketsLeft = count(list)
    def feed(self):
        self.PacketsLeft - 1
